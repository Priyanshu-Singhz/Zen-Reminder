import 'package:flutter/material.dart';

class ContributePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Contribute to App'),
      ),
      body: Center(
        child: Text('Contribute Page'),
      ),
    );
  }
}
